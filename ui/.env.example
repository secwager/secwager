# Copy to .env.local and fill in values
VITE_ENVOY_HOST=http://localhost:8080
VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
# Set to true in local dev only — prod uses the Post Confirmation Lambda instead
VITE_DEV_COMPLETE_REGISTRATION=
